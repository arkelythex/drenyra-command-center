{
  "name": "@drenyra/mission-protocol",
  "version": "0.1.0",
  "description": "Canonical mission protocol — ADAPTER SHIM re-exporting drenyra-ai/missions (single authority). Types, schemas, states, commands, events, errors, and versioning. Transport-agnostic.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "types": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "typescript": "^5.8.0"
  },
  "devDependencies": {
    "vitest": "^4.1.11"
  },
  "dependencies": {
    "drenyra-ai": "file:../../vendored/drenyra-ai-0.2.0.tgz"
  },
  "files": [
    "src"
  ]
}
