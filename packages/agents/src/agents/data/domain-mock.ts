import type { UnifiedAgentEntry } from "../types";

export const DOMAIN_MOCK_AGENTS = [
	// ═══════════════════════════════════════════════
	// Domain Mock (packages/domain/src/drenyra/types.ts — AGENT_TYPES)
	// ═══════════════════════════════════════════════
	{
		id: "cpe-agent",
		name: "CPE Agent",
		system: "domain-mock" as const,
		tier: "tier3" as const,
		parentId: null,
		maySpawn: [],
		isLeaf: true,
		capabilities: ["document-processing", "validation"] as const,
		approvalClass: "not-required" as const,
		supportedSurfaces: ["batch", "automation"] as const,
		drenyraSubagent: null,
		description: "Mock agent for CPE comprobante validation",
		sourcePath: "packages/domain/src/drenyra/types.ts",
	},
	{
		id: "sire-agent",
		name: "SIRE Agent",
		system: "domain-mock" as const,
		tier: "tier3" as const,
		parentId: null,
		maySpawn: [],
		isLeaf: true,
		capabilities: ["sunat-validation", "compliance-audit"] as const,
		approvalClass: "not-required" as const,
		supportedSurfaces: ["batch", "automation"] as const,
		drenyraSubagent: null,
		description: "Mock agent for SIRE book review and validation",
		sourcePath: "packages/domain/src/drenyra/types.ts",
	},
	{
		id: "ledger-agent",
		name: "Ledger Agent",
		system: "domain-mock" as const,
		tier: "tier3" as const,
		parentId: null,
		maySpawn: [],
		isLeaf: true,
		capabilities: ["ledger-review", "validation"] as const,
		approvalClass: "not-required" as const,
		supportedSurfaces: ["batch", "automation"] as const,
		drenyraSubagent: null,
		description: "Mock agent for ledger entry review",
		sourcePath: "packages/domain/src/drenyra/types.ts",
	},
	{
		id: "conciliation-agent",
		name: "Conciliation Agent",
		system: "domain-mock" as const,
		tier: "tier3" as const,
		parentId: null,
		maySpawn: [],
		isLeaf: true,
		capabilities: ["bank-reconciliation", "data-sync"] as const,
		approvalClass: "not-required" as const,
		supportedSurfaces: ["batch", "automation"] as const,
		drenyraSubagent: null,
		description: "Mock agent for bank and ledger conciliation",
		sourcePath: "packages/domain/src/drenyra/types.ts",
	},
	{
		id: "fiscal-reviewer-agent",
		name: "Fiscal Reviewer Agent",
		system: "domain-mock" as const,
		tier: "tier3" as const,
		parentId: null,
		maySpawn: [],
		isLeaf: true,
		capabilities: ["compliance-audit", "risk-analysis"] as const,
		approvalClass: "supervisor" as const,
		supportedSurfaces: ["batch", "automation"] as const,
		drenyraSubagent: null,
		description: "Mock agent for fiscal review and risk assessment",
		sourcePath: "packages/domain/src/drenyra/types.ts",
	},
	{
		id: "evidence-agent",
		name: "Evidence Agent",
		system: "domain-mock" as const,
		tier: "tier3" as const,
		parentId: null,
		maySpawn: [],
		isLeaf: true,
		capabilities: ["evidence-tracking", "document-processing"] as const,
		approvalClass: "not-required" as const,
		supportedSurfaces: ["batch", "automation"] as const,
		drenyraSubagent: null,
		description: "Mock agent for evidence tracking and document linking",
		sourcePath: "packages/domain/src/drenyra/types.ts",
	},
] as const satisfies readonly UnifiedAgentEntry[];
