{
  "name": "@arkelythex/domain-civic",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Arkelythex Civic domain — election entities, vote validation, fraud detection value objects, and repository ports",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@arkelythex/domain": "workspace:*",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^6.0.0",
    "vitest": "^4.0.0"
  }
}
