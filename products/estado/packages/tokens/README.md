# Design Tokens

Tokens are the UI contract: colors, spacing, typography, radii.

Publish tokens as JSON and build outputs (CSS variables + TS exports).
This mirrors public design-token workflows used by Japan's Digital Agency.
