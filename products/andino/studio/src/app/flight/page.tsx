import FlightDashboard from '@/components/flight/FlightDashboard';

export const metadata = { title: 'Flight Dashboard - Andino Studio' };

export default function Page() {
  return <FlightDashboard />;
}
