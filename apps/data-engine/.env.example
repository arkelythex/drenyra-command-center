# =============================================================================
# @arkelythex/data-engine — Python Data Engine env vars
# =============================================================================
# SHARED env vars live in .env.example at repo root.
# This file documents ONLY data-engine-specific vars.
#
# See repo root .env.example for: DATABASE_URL, REDIS_URL, DATA_ENGINE_PORT,
# DATA_ENGINE_URL, POLARS_MAX_THREADS, MAX_UPLOAD_SIZE_MB, CASHFLOW_FORECAST_DAYS
# =============================================================================

DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/arkelythex
REDIS_URL=redis://localhost:6379
REDIS_TTL=3600
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
POLARS_MAX_THREADS=8
MAX_UPLOAD_SIZE_MB=50
CASHFLOW_FORECAST_DAYS=90
