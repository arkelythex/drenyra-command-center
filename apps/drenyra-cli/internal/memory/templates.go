package memory

// Hermes-style seed entries (§ delimiter), not markdown essays.
const defaultMemoryEntries = `Drenyra CLI is the terminal companion for Drenyra App; harness orchestration lives in @arkelythex/harness (TypeScript); Go CLI is HTTP client only
§
Fiscal defaults (RUC, period, org) come from ~/.arkelythex/config.yaml — never store API keys in memory files
§
SUNAT / SIRE / ledger tasks route to fiscal agents; codegen swarms route to ai-swarm-orchestrator`

const defaultUserEntries = `Prefer Spanish (Rioplatense) for explanations; English for code identifiers and CLI strings
§
Use drenyra tui for exploration; drenyra run / exec for scripts and CI
§
IDE cross-session memory: Engram (Gentle AI) project arkelythex — complements CLI MEMORY.md / USER.md`
