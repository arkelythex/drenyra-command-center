/**
 * Taxation Types — Re-export from @drenyra/domain
 *
 * @deprecated Import from @drenyra/domain instead:
 *   import type { TaxDeclaration, IGVSummary } from "@drenyra/domain";
 */

export type {
	TaxPeriod,
	DeclarationType,
	DeclarationStatus,
	TaxDeclaration,
	IGVSummary,
	Detraction,
	TaxCalendar,
} from "@drenyra/domain";
