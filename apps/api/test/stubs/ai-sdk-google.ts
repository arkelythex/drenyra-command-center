/**
 * Stub for @ai-sdk/google package
 * Used in test environment to prevent import errors
 */
import { vi } from 'vitest';

export const google = vi.fn(() => ({}));
export const GoogleGenerativeAI = vi.fn();
