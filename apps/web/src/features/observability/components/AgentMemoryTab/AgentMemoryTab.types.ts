/**
 * Re-exported types for AgentMemoryTab.
 *
 * Canonical type definitions live in features/observability/types.ts.
 * This file provides a local reference so sub-components don't need deep
 * relative imports across the feature boundary.
 */

export type { MemoryEntry, MemoryProfile } from "../../types";
