import type { FC } from "react";

export const InspectorFiscalPanel: FC = () => {
	return (
		<div>
			<p className="text-sm text-[var(--text-tertiary)]">
				Fiscal inspector panel — detalles de la operación fiscal seleccionada.
			</p>
		</div>
	);
};
